/**
 * Address Utilities
 *
 * Re-exported from @block52/poker-vm-sdk for convenience.
 * All utilities now live in the SDK to ensure consistency across clients.
 */

export { isEmptyAddress, isValidPlayerAddress } from "@block52/poker-vm-sdk";
